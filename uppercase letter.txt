#include <stdio.h>
int main(){
    char a;
    scanf("%c",&a);
    if(a>=65&&a<90) {
      printf("upper case letter");  
    }
   else{
       printf("Not Upper case letter");
   }
    return 0;
}